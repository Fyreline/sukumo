# Core web + data layer — see docs/ARCHITECTURE.md §4.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic-settings>=2.2

# Database layer: SQLAlchemy 2.x ORM models (docs/DATA_MODEL.md).
sqlalchemy>=2.0

# Auth (docs/AUTH.md): JWT access + rotating refresh tokens. NO argon2, NO
# password hashing anywhere in this file — Sukumo never stores a password; it
# verifies credentials by proxying to Mishka Hub's own /api/auth/login. The
# absence of an argon2 dependency here is the proof.
pyjwt>=2.9

# --- Dev / test ---
pytest>=8.0
respx>=0.21
