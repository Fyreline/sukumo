# Core web + data layer — see docs/ARCHITECTURE.md §4.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic-settings>=2.2

# Database layer: SQLAlchemy 2.x ORM models (docs/DATA_MODEL.md).
sqlalchemy>=2.0

# Auth (docs/AUTH.md): JWT access + rotating refresh tokens. NO argon2, NO
# password hashing anywhere in this file — Sukumo never stores a password; it
# verifies credentials by proxying to Mishka Hub's own /api/auth/login. The
# absence of an argon2 dependency here is the proof.
pyjwt>=2.9

# Calendar polling (docs/API.md §6, docs/ARCHITECTURE.md §1 clients/calendar.py):
# icalendar (RFC 5545 parsing, handles Apple's published-feed quirks) +
# recurring-ical-events (expands RRULE/EXDATE within a rolling window — the
# `ics` package's recurrence support is weaker, so this pair was chosen
# instead; docs/phases/PHASE-2-ingestion.md).
icalendar>=6.0
recurring-ical-events>=3.0

# Phone-health payload date parsing (docs/API.md §2c) — Path A/Path B send a
# handful of date formats (bare date, space-separated with offset, ISO
# with offset); dateutil.parser handles all of them. Already a transitive
# dep of icalendar, declared directly since app/ingest/health.py and
# app/ingest/events.py import it.
python-dateutil>=2.9

# --- Dev / test ---
pytest>=8.0
respx>=0.21
