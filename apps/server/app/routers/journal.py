"""Journal day / weekly digest read endpoints — docs/MEMORY.md,
docs/phases/PHASE-7-memory.md.

Empty scaffold — built out in Phase 7.
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["journal"])
